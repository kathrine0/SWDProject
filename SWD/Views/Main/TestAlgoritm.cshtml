@model SWD.DataAccess.Model.StringResult
@{
    ViewBag.Title = "TestAlgoritm";
}

<h2>TestAlgoritm</h2>
@{ var results = @Model.Result.Split('v'); }

@foreach (var result in results)
{
    @result
    <br/>
}

